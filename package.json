{
  "name": "iphone-screenshot-inbox-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env.local src/server.mjs",
    "test": "node --test",
    "relay": "bash relay.sh",
    "relay:stop": "bash relay.sh stop",
    "relay:status": "bash relay.sh status"
  },
  "engines": {
    "node": ">=20.10"
  }
}
